{
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.toml",
    "pan-test": "node scripts/pan-test.mjs",
    "check": "node --check worker.js && node --check scripts/run-panapheus.mjs && node scripts/self-check.mjs && node scripts/worker-self-check.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
